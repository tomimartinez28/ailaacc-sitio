// Parámetros que se pueden ajustar sin tocar el resto del código.

// Dos marcas separadas por menos de estos minutos se consideran duplicadas.
export const MINUTOS_DUPLICADO = 5;

// Cantidad mínima de pares Entrada/Salida que se muestran por día.
export const PARES_MINIMOS = 3;

// Colores del Excel de salida (hexadecimal sin #).
export const COLORES = {
  cabecera: '1F4E78',
  impar: 'F8CBAD',
  duplicada: 'FFC000',
  finDeSemana: 'F2F2F2',
  borde: 'BFBFBF',
  enlace: '0563C1',
};

export const FUENTE = { name: 'Arial', size: 10 };
